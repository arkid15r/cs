import { binarySearch } from 'algorithms/searching/binary-search/recursive';
import { runTests } from 'algorithms/searching/binary-search/test/common';

describe('Binary Search Recursive', function () {
  runTests(binarySearch);
});
